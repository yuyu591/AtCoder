#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
#define rep1(i,n) for (int i = 1; i <= (n); i++)
using namespace std;
using ll = long long;

int main() {
    string s; cin >> s;

    string alpha = "abcdefghijklmnopqrstuvwxyz";
    bool flag1 = false;
    bool flag2 = false;
    bool flag3 = true;

    for (auto c : s) {
        if (isupper(c)) flag1 = true;
        else if (islower(c)) flag2 = true;
    }
    
    rep(i,s.size()) {
        rep(k,s.size()) {
            if (i != k && s.at(i) == s.at(k)) {
                flag3 = false;
            }
        }
    }

    if (flag1 && flag2 && flag3) cout << "Yes" << endl;
    else cout << "No" << endl;
    return 0;
}