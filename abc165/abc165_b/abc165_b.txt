#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    ll x;
    cin >> x;

    ll yen = 100;
    int ans = 0;

    while (yen < x) {
        yen += yen / 100;
        ans++;
    }
    cout << ans << endl;
}