#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int k,a,b;
    cin >> k >> a >> b;

    bool exist = false;
    for (int num = a; num <= b; num++) {
        if (num % k == 0) {
            exist = true;
            break;
        }
    }

    if (exist) cout << "OK" << endl;
    else cout << "NG" << endl;
}