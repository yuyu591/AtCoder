#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
#define rep1(i,n) for (int i = 1; i <= (n); i++)
using namespace std;
using ll = long long;

int main() {
    string s; cin >> s;

    cout << "Of";
    for (auto c : s) {
        if (isupper(c)) cout << (char)tolower(c);
        else cout << c;
    }
    cout << endl;
    return 0;
}