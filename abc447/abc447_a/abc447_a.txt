#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int n,m;
    cin >> n >> m;

    if (n % 2 == 0 && m <= n / 2) {
        cout << "Yes" << endl;
    } else if (n % 2 == 1 && m <= n / 2 + 1) {
        cout << "Yes" << endl;
    } else {
        cout << "No" << endl;
    }
}