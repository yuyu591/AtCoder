#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int n;
    cin >> n;

    vector<vector<int>> v(n);
    rep(i,n) {
        int l;
        cin >> l;
        vector<int> a(l);
        rep(k, l) cin >> a.at(k);
        v.at(i) = a;
    }

    int x,y;
    cin >> x >> y;
    cout << v.at(x - 1).at(y - 1) << endl;
    return 0;
}