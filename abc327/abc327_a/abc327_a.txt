#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int n;
    string s;
    cin >> n >> s;

    rep(i, n-1) {
        if ((s.at(i) == 'a' && s.at(i + 1) == 'b')
            || (s.at(i) == 'b' && s.at(i + 1) == 'a')) {
            cout << "Yes" << endl;
            return 0;
        }
    }
    cout << "No" << endl;
    return 0;
}