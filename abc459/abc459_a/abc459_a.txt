#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int x;
    cin >> x;

    string s = "HelloWorld";
    rep(i, 10) {
        if (i == x - 1) continue;
        cout << s.at(i);
    }
    return 0;
}