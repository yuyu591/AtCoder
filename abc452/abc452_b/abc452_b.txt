#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int h,w; cin >> h >> w;

    rep(i,w) cout << "#";
    cout << endl;

    rep(i, h - 2) {
        cout << "#";
        rep(k, w - 2) cout << ".";
        cout << "#" << endl;
    }

    rep(i,w) cout << "#";
    cout << endl;
    return 0;
}