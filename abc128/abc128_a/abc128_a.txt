#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int a,p;
    cin >> a >> p;

    int total = a * 3 + p;
    int ans = total / 2;
    cout << ans << endl;

    return 0;
}