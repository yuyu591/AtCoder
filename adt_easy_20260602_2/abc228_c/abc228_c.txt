#include <bits/stdc++.h>
using namespace std;

#define rep(i,n) for (int i = 0; i < (n); i++)
#define rep1(i,n) for (int i = 1; i <= (n); i++)

#define YES cout<< "Yes" << endl
#define NO cout << "No" << endl
#define YN {cout << "Yes" << endl;} else {cout << "No" << endl;} // if(a==b)YN;
#define dame cout << -1 << endl

using ll = long long;

int main() {
    int n,k; cin >> n >> k;
    vector<int> v(n);
    vector<int> v300(n);

    rep(i,n) {
        int total = 0;
        rep(k,3) {
            int p; cin >> p;
            total += p;
        }
        v.at(i) = total;
        v300.at(i) = total + 300;
    }
    
    sort(v.rbegin(), v.rend());

    rep(i, n) {
        if (v300.at(i) >= v.at(k-1)) YN;
    }

    return 0;
}