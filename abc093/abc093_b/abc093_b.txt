#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int a,b,k;
    cin >> a >> b >> k;

    for (int i = a; i <= b; i++) {
        if ((i <= a + k - 1) || (i >= b - k + 1)) {
            cout << i << endl;
        }
    }
}