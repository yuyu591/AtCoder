#include <bits/stdc++.h>
using namespace std;

#define rep(i,n) for (int i = 0; i < (n); i++)
#define rep1(i,n) for (int i = 1; i <= (n); i++)

#define YES cout<< "Yes" << endl
#define NO cout << "No" << endl
#define YN {cout << "Yes" << endl;} else {cout << "No" << endl;} // if(a==b)YN;
#define dame cout << -1 << endl

using ll = long long;

int main() {
    int n; cin >> n;

    int st = 0, nd = -1;
    string stname = "st", ndname = "nd";
    rep(i,n) {
        string s;
        int t;
        cin >> s >> t;

        if (t > nd) {
            if (t > st) {
                nd = st;
                ndname = stname;
                st = t;
                stname = s;
            } else {
                nd = t;
                ndname = s;
            }
        }
    }
    cout << ndname << endl;
    return 0;
}