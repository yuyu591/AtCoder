#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int h,a;
    cin >> h >> a;

    int res = h;
    int cnt = 0;
    while(res > 0) {
        res -= a;
        cnt++;
    }

    cout << cnt << endl;
}