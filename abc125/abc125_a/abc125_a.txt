#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int a,b,t;
    cin >> a >> b >> t;

    int cnt = (t + 0.5) / a;
    cout << cnt * b << endl;

    return 0;
}