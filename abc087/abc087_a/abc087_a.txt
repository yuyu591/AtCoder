#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int x,a,b;
    cin >> x >> a >> b;

    int res = (x - a) % b;
    cout << res << endl;
    return 0;
}