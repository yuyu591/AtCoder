#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    double d; cin >> d;
    double r = d / 2.0;
    double pi = 3.141592653589793;
    double ans = pi * r * r;
    cout << fixed << setprecision(14) << ans << endl;
    return 0;
}