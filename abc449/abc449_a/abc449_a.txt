#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    double d; cin >> d;
    double ans = d * d * M_PI / 4.0;
    cout << fixed << setprecision(20) << ans << endl;
    return 0;
}