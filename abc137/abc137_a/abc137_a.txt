#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int a,b;
    cin >> a >> b;

    int add = a + b;
    int sub = a - b;
    int mul = a * b;
    
    cout << max({add, sub, mul}) << endl;
    return 0;
}