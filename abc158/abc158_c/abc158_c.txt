#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    int a,b;
    cin >> a >> b;

    for (int i = 1; i < 10001; i++) {
        int A = i * 8 / 100;
        int B = i * 10 / 100;
        if (A == a && B == b) {
            cout << i << endl;
            return 0;
        }
    }
    
    cout << -1 << endl;
    return 0;
}