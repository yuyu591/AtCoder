#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    string s;
    int n;
    cin >> s >> n;

    string ans = s.substr(n, s.size() - n * 2);
    cout << ans << endl;
    return 0;
}