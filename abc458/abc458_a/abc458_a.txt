#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main() {
    string s;
    int n;
    cin >> s >> n;

    int len = s.size();
    rep(i,30) {
        if (i < n || i >= len - n) {
            continue;
        }
        cout << s.at(i);
    }
    cout << endl;
    return 0;
}